unit uFrameColunas;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants, 
  FMX.Types, FMX.Graphics, FMX.Controls, FMX.Forms, FMX.Dialogs, FMX.StdCtrls,
  FMX.Objects, Generics.Collections;

type
  TColumnsFrame = class
  private

  public
    Nome: string;
    Cor: TAlphaColorRec;
    tamanhoPorcent: Single;
end;

type
  TFrame1 = class(TFrame)
    rectGeral: TRectangle;
  private
    { Private declarations }
  public
    { Public declarations }
    constructor Create(AOwner: TComponent; TObjectList<TColumnsFrame>); reintroduce;
  end;

implementation

{$R *.fmx}

{ TFrame1 }

{ TFrame1 }

constructor TFrame1.Create(AOwner: TComponent; TObjectList<TColumnsFrame>);
begin
  inherited Create(AOwner);

end;

end.
